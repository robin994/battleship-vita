# Obsolete vitaGL shader-cache staging directory

This directory is no longer packaged. BattleShip uses Fast3D's validated
linked-program cache in `ux0:data/battleship/shader_cache`; vitaGL must be
built without `HAVE_SHADER_CACHE=1`.
